# Architecture Configuration
# Set to 'x86' for x86_64 systems or 'arm64' for ARM64 systems (e.g., Jetson)
ARCH_TAG=x86

# ROS Configuration
ROS_DOMAIN_ID=0

# User Configuration (automatically set in most cases)
# USER will be set automatically from environment
HOST_USER_UID=1000
HOST_USER_GID=1000

# Model and Engine Paths [Optional]
# ESS Depth Model Path 
ESS_MODEL_PATH=/home/${USER}/ess_model

# Display Configuration (automatically set from environment)
# DISPLAY will be set automatically from environment